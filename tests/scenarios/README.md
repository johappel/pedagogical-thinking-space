# Scenario Tests

This folder contains manual scenario checks for future agent-behavior testing.

The first stage is descriptive only.

These files define expected behavior and simple fail signals before any automated LLM evaluation is introduced.
// sandbox-workspace-scope — Host half.
// Pure UI package: the client half scopes the sidebar workspace list. The host
// needs only a no-op apply so the row exists for the loader.
export function apply() {}
